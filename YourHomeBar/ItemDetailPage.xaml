<Page
    x:Name="pageRoot"
    x:Class="YourHomeBar.ItemDetailPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YourHomeBar"
    xmlns:data="using:YourHomeBar.Data"
    xmlns:common="using:YourHomeBar.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid DataContext="{Binding Item}"
        d:DataContext="{Binding Groups[0].Items[0], Source={d:DesignData Source=/DataModel/SampleData.json, Type=data:SampleDataSource}}">
        <Grid.Background>
            <ImageBrush ImageSource="assets/wood.jpg"/>
        </Grid.Background>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--
            TODO: Content should be placed within the following grid 
                  to show details for the current item
        -->
        <Grid Grid.Row="1" x:Name="contentRegion"> 
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="80"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="700"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Recipe" FontSize="42" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="0" Grid.Column="3"/>

            <Grid Grid.Row="1" Grid.Column="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock x:Name="itemAlcoholType" Text="Alcohol Type:" FontSize="20"/>
                    <TextBlock x:Name="itemGlassType" Text="Glass Type:" FontSize="20"/>
                    <TextBlock x:Name="itemIngredreint" Text="Ingredient:" FontSize="20"/>
                </StackPanel>
            </Grid>
            
            <Grid Grid.Column="3" Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="The most common Liquor:" Grid.Column="0" FontSize="20" Margin="20" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <TextBlock Text="ALCOHOL TYPE" Grid.Column="1" FontSize="20" Margin="20" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
                        
            <Image Source="{Binding ImagePath}" Grid.Row="1" Grid.Column="1" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}"/>
            
            <Button x:Name="VoteOnRecipe" Content="Vote on this recipe" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Center">
                <Button.Flyout>
                    <Flyout Opened="flyout_Opened">
                        <StackPanel Style="{StaticResource SettingsFlyoutSectionStyle}" Width="300">
                            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,25" Text="Voting Criteria" HorizontalAlignment="Center"/>
                            <TextBlock Style="{StaticResource BodyTextBlockStyle}" Margin="0,0,0,25" TextWrapping="Wrap" HorizontalAlignment="Center">
                                We vote on our recipies under two conditions:
                            </TextBlock>
                            <TextBlock Style="{StaticResource BodyTextBlockStyle}" Margin="0,0,0,25" TextWrapping="Wrap" HorizontalAlignment="Center">
                                First - Overall rating
                            </TextBlock>
                            <TextBlock Style="{StaticResource BodyTextBlockStyle}" Margin="0,0,0,25" TextWrapping="Wrap" HorizontalAlignment="Center">
                                Second - whether or not you classify this drink recipe as being a classic drink
                            </TextBlock>
                            
                            <StackPanel HorizontalAlignment="Center">
                                <RadioButton Content="5 Stars"/>
                                <RadioButton Content="4 Stars"/>
                                <RadioButton Content="3 Stars"/>
                                <RadioButton Content="2 Stars"/>
                                <RadioButton Content="1 Stars"/>
                            </StackPanel>
                            
                            <ToggleSwitch OnContent="This is a Classic Drink" OffContent="This is a Not a Classic Drink" HorizontalAlignment="Center"/>
                            <Button Content="Submit" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
            
		</Grid>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{Binding Title}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
    </Grid>
</Page>
